# ----------------------------------------------------------------------
# Project Context (schema-enforced)
# ----------------------------------------------------------------------
# Auto-generated by kerby. Fill in the sections below.
#
# Rules:
# - Sections `project`, `runtime`, and `stack` must exist.
# - These sections are primarily maintained by humans.
# - AI agents may freely extend ONLY the `agentNotes` section, and may
#   edit core sections only when explicitly instructed.
# ----------------------------------------------------------------------

version: 1.0.0
lastUpdated: "2026-08-02"

project:
  name: "kerby"
  description: "The gate guardian for agentic coding — a loadable rule-corpus + opt-in guardrail hooks (GATE → WEIGH → VERDICT); domain-blind engine + self-contained rulebooks."
  type: "agent-skill"
  visibility: public

runtime:
  primary: "bash"         # hook enforcers are the executable surface
  language: "bash"
  target: ""

stack:
  frameworks: {}
  tools:
    python: "scripts/check-skill-compat.py, skills/kerby/resources/scripts/validate-rulebook.py"
    bash: "rulebook hook enforcers + their .test.sh self-checks"
  aiProviders: []

entryPoints: ["skills/kerby/SKILL.md"]
directories:
  source: skills/kerby/
  public: ""
  distribution: ""

ai:
  enabled: false
  usage: []
  notes: []
  memoryPolicy:
    logTo: .kerby/memory.log
    maxEntries: 200
  planThreshold: 4

knowledge:
  enabled: true

preferences: {}
status:
  editableBy: [human, ai]
  changePolicy: append-log
  lastEditedBy: ai

agentNotes: {}
